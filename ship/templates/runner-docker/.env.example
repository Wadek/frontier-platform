# Registration token from GitHub (repo → Settings → Actions → Runners → New).
# Tokens expire; recreate .env when re-registering.
REPO_URL=https://github.com/<owner>/<repo>
RUNNER_TOKEN=
RUNNER_NAME=frontier-linux-docker
LABELS=self-hosted,Linux,X64
