# Build output
/frontier
/frontier.exe
/frontier-git
/frontier-git.exe
/frontier-mcp
/frontier-mcp.exe
dist/
*.exe
*.o
*.hi

# Habitat runtime at the repo root (ledgers, hooks, installed binaries).
# Anchored: an unanchored "runtime/" also hid ship/internal/runtime (Go package)
# and broke CI (go mod tidy then fetched it as an unpublished module).
/runtime/

# Local scratch
.frontier/
node_modules/
__pycache__/
.DS_Store

# Local cleanup working list
.cleanup-terms.md

# Local security-MCP fixtures; not part of the public tree.
ship/internal/secmcp/testdata/
