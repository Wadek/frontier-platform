# Method notes

Full table lives in `control/english/LEARNING.md`. This file is the short agent card.

## Select

| Kind | Methods |
|---|---|
| session end | `debrief` |
| fact / API / claim | `generate` then `retrieval`; persist misses with `spaced` |
| procedure | `faded_example` then `deliberate` on the miss |
| similar procedures | `interleave` |
| why / mechanism | `elaborate` then `teachback` |
| speech / YouTube | `listen` (optional `dual` if visuals matter) |
| teacher pass | `seed` |

## Drill unit

One item. Answer hidden. Correct in one line. Next item or stop.

## YouTube

See `sources-youtube.md`. Method IDs stay `listen` / `dual`.
